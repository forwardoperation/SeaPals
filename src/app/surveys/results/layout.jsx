export const metadata = {
  alternates: { canonical: "/surveys/results" },
  robots: { index: false, follow: false },
};

export default function SurveyResultsLayout({ children }) {
  return children;
}
